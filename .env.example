# Archon Gatekeeper URL
ARCHON_GATEKEEPER_URL=https://archon.technology

# Transport mode: 'stdio' (local) or 'http' (remote)
TRANSPORT=stdio

# HTTP server settings (when TRANSPORT=http)
PORT=4251
HOST=0.0.0.0
